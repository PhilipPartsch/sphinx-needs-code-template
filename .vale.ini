StylesPath = styles
MinAlertLevel = suggestion

Packages = proselint, write-good #Google, Microsoft, proselint, alex

# Here's were we define the exceptions to use in *all* `BasedOnStyles`.
Vocab = merge_dicts

[*.rst]
BasedOnStyles = Vale, proselint, write-good #Google, Microsoft, proselint, alex
